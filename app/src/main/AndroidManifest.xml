<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fndt.alarm">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".model.util.AlarmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.AlarmActivity"
            android:exported="false"
            android:screenOrientation="sensorPortrait"
            android:showOnLockScreen="true" />

        <receiver
            android:name=".model.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="${applicationId}.ALARM_ACTION" />
                <action android:name="${applicationId}.STOP_ALARM" />
                <action android:name="${applicationId}.SETUP_ALARM" />
                <action android:name="${applicationId}.CANCEL_ALARM" />
                <action android:name="${applicationId}.SNOOZE_ALARM" />
            </intent-filter>
        </receiver>
        <service
            android:name=".model.AlarmService"
            android:exported="false" />
    </application>

</manifest>